// 欧拉路径与欧拉回路

// 定义：
// 每条边恰好走一次
// 起点=终点 -> 欧拉回路
// 起点≠终点 -> 欧拉路径

// 有向图存在欧拉回路条件：
// 1. 忽略入度=出度=0的点，其余点满足 入度=出度
// 2. 图强连通（任意两点可达）

// Hierholzer算法（构造）：
// 1. 从任意点s出发，沿未走过的边一直走，直到回到s（主环）
// 2. 若仍有未走边，则该边一定连到主环某点x
// 3. 从x出发再走未走边，形成一个子环
// 4. 将子环插入主环
// 5. 重复直到所有边用完 -> 得到欧拉回路